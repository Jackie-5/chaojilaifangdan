//-Created by JackieWu on 12/22/15.
| <?js if(it.length > 0){ ?>
| <?js var day=''; ?>
.wait
    | <?js it.forEach(function(item,k){ ?>
    .box
        .wait-title
            .title-bg
                span @{item.order_type_name}
        | <?js item.list.forEach(function(listItem, i){ ?>
        | <?js if(!!~listItem.diff_days.toString().indexOf('-')){ ?>
        | <?js     day = '过期'+listItem.diff_days.toString().split('-')[1]+'天' ?>
        | <?js  }else if(listItem.diff_days === 0){ ?>
        | <?js     day = '今日' ?>
        | <?js  }else { ?>
        | <?js     day = '还剩'+listItem.diff_days+'天' ?>
        | <?js  } ?>
        .wait-list.J_wait-list
            .list-box
                .list-left
                    i.list-icon-01
                    span 客户姓名
                .list-right
                    span @{listItem.customer_name}
            .list-box
                .list-left
                    i.list-icon-02
                    span 当前级别
                .list-right
                    span @{listItem.level}级客户
                    // 后端接口无法直接的更改等级 所以暂时注释掉
                    //a(href="#")
                    //    i.right-icon-3
            .list-box
                .list-left
                    i.list-icon-03
                    span 最新接触
                .list-right
                    span @{listItem.lasttime}
            .list-box
                .list-left
                    i.list-icon-04
                    span 邀约来访
                .list-right.J_customer-mobile
                    a
                        i.right-icon-1
                    a
                        i.right-icon-2
            .list-box
                .list-left
                    i.list-icon-05
                    span 倒计时
                .list-right.J_customer-date-input
                    span @{day}
                    label(for="input-@{k}-@{i}")
                        input(type="date", id="input-@{k}-@{i}")
                        i.right-icon-3
        | <?js }); ?>
    | <?js }); ?>
| <?js } ?>
