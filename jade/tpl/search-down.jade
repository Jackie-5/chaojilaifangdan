//-Created by JackieWu on 12/23/15.
//-定金
| <?js var next_order_type = '',day='',area= '',house_room = '',house_floor ='',house_number=''; ?>
| <?js if(it.data.next_order_type == 1){ ?>
| <?js     next_order_type= '再次来访'; ?>
| <?js  }else if(it.data.next_order_type == 2){ ?>
| <?js     next_order_type= '付意向金'; ?>
| <?js  }else if(it.data.next_order_type == 3){ ?>
| <?js     next_order_type= '付定金';?>
| <?js  }else if(it.data.next_order_type == 4){ ?>
| <?js      next_order_type= '签约'; ?>
| <?js  }else if(it.data.next_order_type == 5){ ?>
| <?js      next_order_type= '待付款'; ?>
| <?js  }else if(it.data.next_order_type == 6){ ?>
| <?js      next_order_type= '确认付款'; ?>
| <?js  }else if(it.data.next_order_type == 7){ ?>
| <?js      next_order_type= '带交房'; ?>
| <?js  } ?>

| <?js if(it.data.house_area != 0){area = it.data.house_area} ?>
| <?js if(it.data.house_number != 0){house_number = it.data.house_number} ?>
| <?js if(it.data.house_floor != 0){house_floor = it.data.house_floor} ?>
| <?js if(it.data.house_room != 0){house_room = it.data.house_room} ?>
.search-intention
    .intention-list
        .inten-name
            i.s-i-icon-1
            span 项目名称
        .inten-input
            .project.J_inten-project @{it.house_name}
    .intention-list
        .inten-name
            i.s-i-icon-7
            span 购房房号
        .inten-input.house-number
            input.J_inten-number.input-center(type='text',value="@{house_number}")
            .separated 栋
            input.J_inten-floor.input-center(type='text',value="@{house_floor}")
            .separated 层
            input.J_inten-room.input-center(type='text',value="@{house_room}")
            .separated 室
    .intention-list
        .inten-name
            i.s-i-icon-2
            span 建筑面积
        .inten-input
            input.J_inten-area(type='text',value="@{area}")
    .intention-list
        .inten-name
            i.s-i-icon-3
            span 客户姓名
        .inten-input
            input.J_inten-name(type='text',value="@{it.data.customer_name}")
    .intention-list
        .inten-name
            i.s-i-icon-4
            span 联系电话
        .inten-input
            input.J_inten-phone(type='tel',value="@{it.data.customer_mobile}")
    .intention-list
        .inten-name
            i.s-i-icon-5
            span 下一步进度
        .inten-cont
            .border.J_drop
                i
                span(data-value="@{it.data.next_order_type}") @{next_order_type}
            .drop.J_drop-show.hide
                span(data-value="2") 再次来访
                span(data-value="3") 付意向金
                span(data-value="4") 付定金
                span(data-value="5") 签约
                span(data-value="6") 付款

    .intention-list
        .inten-name
            i.s-i-icon-6
            span 倒计时
        .time-box
            .time
                span.J_diff-days @{it.data.diff_days}
            label.btn-icon
                input#btn-time(type="date")
.search-inten-btn
    span.J_cancel 退订
    span.J_submit 提交
.search-tips 注：退订后将被降为D级客户
